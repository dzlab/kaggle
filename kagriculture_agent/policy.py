class Policy:
    """Minimal import-compatible policy placeholder for Task 1."""

    def act(self, obs):
        """Return a deterministic placeholder action until policy work lands."""
        del obs
        return 0
